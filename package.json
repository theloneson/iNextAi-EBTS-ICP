{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "iNextAi-EBTS",
  "scripts": {
    "build": "bun run build --workspaces --if-present",
    "prebuild": "bun run prebuild --workspaces --if-present",
    "pretest": "bun run prebuild --workspaces --if-present",
    "start": "bun start --workspaces --if-present",
    "test": "bun test --workspaces --if-present",
    "dev": "bun run dev --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/iNextAi-EBTS-frontend"
  ]
}
